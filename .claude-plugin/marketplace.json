{
  "name": "supacode-skills",
  "description": "Skills for driving the Supacode macOS app (supacode.sh) from Claude Code — CLI control of worktrees/tabs/sessions and the plan → implement → verify → PR feature workflow. Requires Supacode.",
  "owner": {
    "name": "Tate Jennings"
  },
  "plugins": [
    {
      "name": "supacode",
      "source": "./plugins/supacode",
      "description": "Requires the Supacode macOS app (supacode.sh). Worktree/tab/session control via the supacode CLI, plus the plan → implement → verify → PR workflow skills."
    }
  ]
}
